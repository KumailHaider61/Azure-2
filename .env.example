# Copy this file to .env.local for local development
# Or set these environment variables in Azure App Service (Configuration -> Application settings)

# Public API base URL (if used)
NEXT_PUBLIC_API_URL=

# Database (if used)
DATABASE_URL=

# Auth / session secrets (if used)
NEXTAUTH_URL=
NEXTAUTH_SECRET=

# Port (Azure will set this automatically; you can leave it empty)
PORT=
